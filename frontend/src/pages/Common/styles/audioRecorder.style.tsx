import styled from "styled-components";

const recorderContainer = styled.div`
  grid-column: 1 / -1;
  grid-row: -2 /-1;

  justify-self: center;
  font-size: 18px;
`;

export { recorderContainer };
